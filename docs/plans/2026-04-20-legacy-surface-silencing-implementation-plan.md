# Legacy Surface Silencing Implementation Plan

## Goal

Make `zhaocai-IP-center` evaluate as the new product surface, not as the old v2.0 workbench.

## Steps

1. Replace runtime navigation and stage-agent shortcuts with agent-route-first navigation.
2. Remove runtime "legacy tools" wording and old v2.0 framing from active UI.
3. Embed the real IP extraction workflow inside the IP extraction agent.
4. Embed the real creator profile editor inside the creator-profile agent.
5. Embed direction/topic/candidate execution sections inside the topic-direction agent.
6. Redirect old workbench routes to the new agent routes.
7. Run targeted typecheck and smoke validation.
